请基于我上传的参考图，用 Three.js 程序化建模生成这个单个游戏资产。

要求：

1. 模型要尽量详细、好看，不能只是简单方块拼接。
   - 要提取参考图里的主要结构、大轮廓、层级关系、装饰细节。
   - 大物体要拆成多个结构层：主体、顶部、底座、门窗、装饰、边框、植物/灯/栏杆等小细节。
   - 要有足够多的小部件，让模型看起来丰富。

2. 必须带材质。
   - 每个 mesh 都必须绑定材质。
   - 材质必须使用 GLB 兼容的 MeshStandardMaterial 或 MeshPhysicalMaterial。
   - 每个材质都要有 name、color、roughness、metalness。
   - 不要只依赖灯光、CSS、后处理或网页效果来表现材质。
   - 不要使用 GLB 容易丢失的 ShaderMaterial、CSS2D、CSS3D 或只存在网页里的效果。

3. 必须生成可预览的 Three.js HTML。
   - 要有 OrbitControls。
   - 要有合适的相机角度。
   - 灯光不要过曝，toneMappingExposure 控制在 0.55 到 0.75 之间。
   - 背景可以是浅灰或米白，不要纯白刺眼。

4. 必须提供 GLB 下载。
   - 网页里可以有“下载 GLB 模型”按钮。
   - 但是最终回复里必须额外给我一个文字下载链接，格式类似：
     [下载带材质 GLB 模型](sandbox:/mnt/data/xxx.glb)
   - 不要只让我点网页里的按钮。

5. 在给我下载链接之前，必须检查 GLB。
   - 检查 mesh 数量。
   - 检查 material 数量。
   - 检查是否有 mesh 没有材质。
   - 检查 GLB 文件里是否真的包含材质。
   - 如果没有材质，不要把文件发给我，先修复。

6. 最终交付必须包含：
   - Three.js HTML 文件下载链接
   - 带材质 GLB 文件下载链接
   - 简短检查结果，例如：
     Mesh 数量：xx
     材质数量：xx
     缺少材质的 Mesh：0
     GLB 已包含材质：是

请直接生成，不要只生成图片，不要只给代码片段。